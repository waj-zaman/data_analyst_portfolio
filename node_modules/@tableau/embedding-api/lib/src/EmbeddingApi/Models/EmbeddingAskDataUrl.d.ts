import { AskDataSettings, CustomParameter } from '@tableau/api-external-contract-js';
import { EmbeddingUrlBuilder } from './EmbeddingUrlBuilder';
export declare function createAskDataUrl(src: string, options: AskDataSettings, embeddingId: number, customParams: CustomParameter[]): URL;
export declare class EmbeddingAskDataUrlBuilder extends EmbeddingUrlBuilder {
    constructor(_url: URL);
    /**
     * Sanitizes parameter values before they are added to the search params.
     * @param parameterName The name of the parameter. Some parameters require special handling.
     * @param value The raw value of the parameter.
     */
    protected sanitizeParameterValue(parameterName: string, value: unknown): string;
}
//# sourceMappingURL=EmbeddingAskDataUrl.d.ts.map