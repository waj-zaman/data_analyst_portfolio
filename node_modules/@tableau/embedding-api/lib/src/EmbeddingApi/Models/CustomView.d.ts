import * as Contract from '@tableau/api-external-contract-js';
import { CustomViewImpl } from '@tableau/api-shared-js';
import { EmbeddingWorkbookImpl } from '../Impl/EmbeddingWorkbookImpl';
import { Workbook } from './Workbook';
export declare class CustomView implements Contract.CustomView {
    private _customViewImpl;
    private _workbookImpl;
    constructor(_customViewImpl: CustomViewImpl, _workbookImpl: EmbeddingWorkbookImpl);
    get name(): string;
    set name(name: string);
    get shared(): boolean;
    set shared(shared: boolean);
    get default(): boolean;
    set default(isDefault: boolean);
    get ownerName(): string;
    get url(): string;
    get workbook(): Workbook;
    saveAsync(): Promise<CustomView>;
}
//# sourceMappingURL=CustomView.d.ts.map