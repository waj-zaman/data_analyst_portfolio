import { AskData } from '@tableau/api-external-contract-js';
import { TableauWebComponent } from './TableauWebComponent';
/**
 * Represents the entry point for the `<tableau-ask-data>` custom HTML element.
 * This class is specifically focused on transferring information between the HTML and
 * the AskData, so it should have as little logic as possible.
 */
export declare class TableauAskData extends TableauWebComponent implements AskData {
    constructor();
    static get observedAttributes(): string[];
    protected updateRenderingIfInitialized(src?: string): Promise<void>;
    protected updateRendering(src?: string): Promise<void>;
    private constructOptions;
    get origin(): string | undefined;
    set origin(v: string | undefined);
    get showEmbed(): boolean;
    set showEmbed(v: boolean);
    get showPin(): boolean;
    set showPin(v: boolean);
    get showShare(): boolean;
    set showShare(v: boolean);
    get showSave(): boolean;
    set showSave(v: boolean);
}
//# sourceMappingURL=TableauAskData.d.ts.map