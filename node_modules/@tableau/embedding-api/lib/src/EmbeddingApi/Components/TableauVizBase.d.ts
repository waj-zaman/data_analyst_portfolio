import * as Contract from '@tableau/api-external-contract-js';
import { Model, NotificationId } from '@tableau/api-internal-contract-js';
import { VizImpl } from '../Impl/VizImpl';
import { TableauWebComponent } from './TableauWebComponent';
export declare type ShouldRaiseNotificationFunc = (model: Model) => boolean;
export declare type HandleNotificationFunc = (model: Model) => void;
export declare type EventHandlerFn = [NotificationId, ShouldRaiseNotificationFunc, HandleNotificationFunc];
export declare type AttributeEventType = [Contract.VizSharedAttributes | Contract.VizAttributes | Contract.VizAuthoringAttributes, Contract.EmbeddingTableauEventType];
/**
 * This class is specifically focused on transferring information between html and viz
 * and giving the user an entry point into the viz model
 * It should have as little logic as possible
 */
export declare abstract class TableauVizBase extends TableauWebComponent {
    static VizAttributeDefaults: {
        device: Contract.DeviceType;
        toolbar: Contract.Toolbar;
    };
    private _vizImpl;
    protected abstract createFrameUrlWithParams(): URL;
    protected abstract getAttributeEvents(): AttributeEventType[];
    disconnectedCallback(): void;
    static get observedAttributes(): string[];
    protected updateRenderingIfInitialized(src?: string): Promise<void>;
    protected updateRendering(src?: string): Promise<void>;
    private createAndInitializeVizImpl;
    private registerAttributeEvents;
    initializeEvents(): void;
    protected getRegisteredEvents(): EventHandlerFn[];
    get touchOptimize(): boolean;
    set touchOptimize(v: boolean);
    protected get vizImpl(): VizImpl;
    get hideEditInDesktopButton(): boolean;
    set hideEditInDesktopButton(v: boolean);
    get suppressDefaultEditBehavior(): boolean;
    set suppressDefaultEditBehavior(v: boolean);
    get disableVersionCheck(): boolean;
    set disableVersionCheck(v: boolean);
    getCurrentSrcAsync(): Promise<string>;
    resize(): void;
}
export declare function attributeToEnumKey(value: string | null): string;
//# sourceMappingURL=TableauVizBase.d.ts.map