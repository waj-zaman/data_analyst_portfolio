import * as Contract from '@tableau/api-external-contract-js';
import { EmbeddingErrorCodes } from '@tableau/api-external-contract-js';
export declare class VizLoadErrorEvent implements Contract.VizLoadErrorEvent {
    private _errorCode;
    private _message;
    get message(): string;
    get errorCode(): EmbeddingErrorCodes;
    constructor(errorCode: EmbeddingErrorCodes, message: string);
}
//# sourceMappingURL=VizLoadErrorEvent.d.ts.map