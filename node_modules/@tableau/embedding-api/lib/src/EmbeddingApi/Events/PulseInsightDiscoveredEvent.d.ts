import * as Contract from '@tableau/api-external-contract-js';
export declare class PulseInsightDiscoveredEvent implements Contract.PulseInsightDiscoveredEvent {
    private _id;
    private _characterization;
    private _markup;
    private _question;
    private _score;
    private _type;
    private _version;
    get id(): string;
    get characterization(): string;
    get markup(): string;
    get question(): string;
    get score(): number;
    get type(): string;
    get version(): number;
    constructor(id: string, characterization: string, markup: string, question: string, score: number, type: string, version: number);
}
//# sourceMappingURL=PulseInsightDiscoveredEvent.d.ts.map